# Avatar durable execution engine — runtime dependencies.
# (Editable install `pip install -e .` reads pyproject.toml; this file is for
# plain `pip install -r requirements.txt` and the Docker image.)
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy[asyncio]>=2.0
pydantic>=2.6
httpx>=0.27
aiosqlite>=0.19
# Postgres (production / docker compose):
asyncpg>=0.29
psycopg[binary]>=3.1
